using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cyuuru : MonoBehaviour
{
    public enum Type{//Typeという名前のパブリック設定の列挙型を作成
        Tuna, chicken, Bonito
    }
    public Type type;//パブリック設定でType型の変数「type」を作成
}
